<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<!--    package="com.example.madcampproj1">  &lt;!&ndash; ✅ 여기에 실제 패키지명 추가 &ndash;&gt;-->

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--네이버 현재 위치 접근 권한-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/new_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/new_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MadCampProj1"
        tools:targetApi="31">

        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="g1h8nbgw10" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MadCampProj1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

    <!--        android:icon="@mipmap/ic_launcher"-->
    <!--        android:label="@string/app_name"-->
    <!--        android:roundIcon="@mipmap/ic_launcher_round"-->

<!--<meta-data-->
<!--android:name="com.naver.maps.map.CLIENT_ID"-->
<!--android:value="g1h8nbgw10" />  &lt;!&ndash; 네이버_클라이언트_ID_입력  &ndash;&gt;-->